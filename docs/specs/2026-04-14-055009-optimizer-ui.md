```yaml
spec_metadata:
  goal: "Stage B: Optimizer UI — Plan grid page, suggestion sidebar, recipe cards with pantry chips, config panel, nav link"
  constraints:
    - "All code in optimizer/ subdirectories per fork isolation rules"
    - "No new backend work — all required APIs exist from Stage A"
    - "Must use existing meal plan CRUD API (api.mealplans.*)"
    - "Must use existing VueDraggable dependency for drag-and-drop"
    - "Frontend only: Nuxt 4, Vue 3, Vuetify 4, TypeScript"
    - "Sidebar nav modification is a known upstream touch point (DefaultLayout.vue)"
  non_goals:
    - "Mobile-responsive layout (Stage D)"
    - "Shopping list enhancements or deficit visualization (Stage C)"
    - "New backend endpoints or migrations"
    - "Touch/swipe gesture support"
    - "Onboarding wizard"
    - "Expiration warnings on pantry page"
  timestamp: "2026-04-14T05:50:09"
  confidence: medium
  survey_consumed: false

current_state:
  summary: >
    Stage A infrastructure is complete: scoring engine (pure TS), Vue composable wrapper,
    optimizer API client (pantry CRUD, config GET/PUT, recipe-foods projection), and all
    TypeScript types. The existing meal plan system provides full CRUD with date-range queries
    and VueDraggable-based drag-and-drop in the edit view. The pantry page establishes the
    optimizer page pattern under /g/[groupSlug]/optimizer/.
  relevant_files:
    - path: "frontend/app/composables/optimizer/scoring-engine.ts"
      purpose: "Pure TS scoring engine — scoreRecipes() returns ScoredRecipe[] with breakdown + pantry matches"
      reuse_potential: high
    - path: "frontend/app/composables/optimizer/use-optimizer-scoring.ts"
      purpose: "Vue composable wrapping scoring engine with reactive refs"
      reuse_potential: high
    - path: "frontend/app/composables/optimizer/types.ts"
      purpose: "ScoringWeights, RecipeFoodData, ScoredRecipe, PantryMatchDetail, PantryItemScoring"
      reuse_potential: high
    - path: "frontend/app/lib/api/user/optimizer-pantry.ts"
      purpose: "OptimizerApi class — pantry CRUD, config GET/PUT, getRecipeFoods()"
      reuse_potential: high
    - path: "frontend/app/lib/api/types/optimizer.ts"
      purpose: "All optimizer API types including OptimizerConfigOut, RecipeFoodProjection"
      reuse_potential: high
    - path: "frontend/app/lib/api/types/meal-plan.ts"
      purpose: "PlanEntryType, CreatePlanEntry, ReadPlanEntry, UpdatePlanEntry, RecipeSummary"
      reuse_potential: high
    - path: "frontend/app/composables/use-group-mealplan.ts"
      purpose: "useMealplans(range) — CRUD actions + reactive mealplans list, watches range"
      reuse_potential: high
    - path: "frontend/app/pages/household/mealplan/planner.vue"
      purpose: "Existing mealplan planner parent — date range picker, view/edit tabs, shopping list dialog"
      reuse_potential: medium
    - path: "frontend/app/pages/household/mealplan/planner/edit.vue"
      purpose: "Existing mealplan edit view with VueDraggable drag-and-drop pattern"
      reuse_potential: medium
    - path: "frontend/app/pages/g/[groupSlug]/optimizer/pantry.vue"
      purpose: "Existing optimizer page pattern — useSeoMeta, useUserApi, CRUD operations"
      reuse_potential: medium
    - path: "frontend/app/components/optimizer/PantryItemRow.vue"
      purpose: "Existing optimizer component pattern — props, emits, reactive editing"
      reuse_potential: low
    - path: "frontend/app/components/Layout/DefaultLayout.vue"
      purpose: "Sidebar navigation — topLinks array with SideBarLink objects"
      reuse_potential: high
    - path: "frontend/app/components/Domain/Recipe/RecipeCardMobile.vue"
      purpose: "Compact recipe card with image, name, description, tags — pattern reference"
      reuse_potential: medium
    - path: "frontend/app/components/Domain/Recipe/RecipeDialogAddToShoppingList.vue"
      purpose: "Existing dialog for adding recipes to shopping list — reuse directly"
      reuse_potential: high
  patterns_identified:
    - "Optimizer pages: /g/[groupSlug]/optimizer/<feature>.vue with useSeoMeta + useUserApi"
    - "Optimizer components: frontend/app/components/optimizer/<Name>.vue with typed props/emits"
    - "Composable pattern: reactive refs + computed + API calls, return object with methods"
    - "Sidebar links: SideBarLink objects in topLinks computed array in DefaultLayout.vue"
    - "VueDraggable: v-model array, group name, handle class, @end callback"
    - "i18n: keys under optimizer.<feature>.* namespace in en-US.json"
    - "Meal plan CRUD: useMealplans(range) with createOne/updateOne/deleteOne actions"

gaps:
  exists:
    - component: "Scoring engine"
      location: "frontend/app/composables/optimizer/scoring-engine.ts"
      notes: "Complete — scoreRecipes() accepts candidates, planned, pantry, weights, budget"
    - component: "Scoring composable"
      location: "frontend/app/composables/optimizer/use-optimizer-scoring.ts"
      notes: "Complete — reactive wrapper with set* methods"
    - component: "Optimizer types"
      location: "frontend/app/composables/optimizer/types.ts"
      notes: "ScoringWeights, RecipeFoodData, ScoredRecipe, PantryMatchDetail, PantryItemScoring"
    - component: "API client"
      location: "frontend/app/lib/api/user/optimizer-pantry.ts"
      notes: "OptimizerApi with pantry CRUD, config GET/PUT, getRecipeFoods()"
    - component: "API types"
      location: "frontend/app/lib/api/types/optimizer.ts"
      notes: "All response types including OptimizerConfigOut, RecipeFoodProjectionResponse"
    - component: "Meal plan API"
      location: "frontend/app/composables/use-group-mealplan.ts"
      notes: "useMealplans(range) with full CRUD — reuse directly"
    - component: "VueDraggable"
      location: "frontend/package.json"
      notes: "Already a dependency, used in mealplan edit view"
    - component: "RecipeDialogAddToShoppingList"
      location: "frontend/app/components/Domain/Recipe/RecipeDialogAddToShoppingList.vue"
      notes: "Existing dialog for shopping list generation — reuse directly"
    - component: "Sidebar pantry link"
      location: "frontend/app/components/Layout/DefaultLayout.vue:249-254"
      notes: "Pantry link pattern already established in topLinks"
  partial:
    - component: "Sidebar navigation"
      location: "frontend/app/components/Layout/DefaultLayout.vue:224-292"
      missing: "Add planner link to topLinks after pantry link"
    - component: "i18n keys"
      location: "frontend/app/lang/messages/en-US.json:1484-1517"
      missing: "Add optimizer.planner.* namespace with all UI text keys"
  missing:
    - component: "Planner page"
      rationale: "No optimizer planner page exists — this is the main deliverable"
    - component: "PlanGrid component"
      rationale: "No grid component for days × entry types layout"
    - component: "PlanSlot component"
      rationale: "No individual slot component for meal plan cell"
    - component: "SuggestionSidebar component"
      rationale: "No sidebar for displaying ranked recipe suggestions"
    - component: "OptimizerRecipeCard component"
      rationale: "No card showing scoring breakdown and pantry match chips"
    - component: "ConfigPanel component"
      rationale: "No panel for adjusting scoring weights"
    - component: "use-optimizer-planner composable"
      rationale: "No orchestration layer for draft plan state, scoring, and API coordination"

specification:
  files:
    # ── Composable ──
    - path: "frontend/app/composables/optimizer/use-optimizer-planner.ts"
      action: create
      purpose: "Orchestrates planner state: draft entries, data loading, scoring bridge, meal plan CRUD, and shopping list trigger"
      signature: |
        import type { Ref, ComputedRef } from "vue";
        import type { ReadPlanEntry, CreatePlanEntry, PlanEntryType } from "~/lib/api/types/meal-plan";
        import type { RecipeFoodData, ScoredRecipe, PantryItemScoring } from "./types";
        import type { OptimizerConfigOut } from "~/lib/api/types/optimizer";

        /**
         * Draft entry before save — keyed by "date|entryType".
         * Tracks whether it maps to an existing ReadPlanEntry (update) or is new (create).
         */
        export interface DraftPlanEntry {
          date: string;                         // "YYYY-MM-DD"
          entryType: PlanEntryType;
          recipeId: string | null;
          recipeName: string | null;            // display only
          recipeSlug: string | null;            // for image lookup
          existingEntryId: number | null;       // non-null if editing an existing entry
        }

        export interface DateRange {
          start: Date;
          end: Date;
        }

        export interface UsePlannerReturn {
          // Reactive state
          dateRange: Ref<DateRange>;
          days: ComputedRef<Date[]>;
          draftEntries: Ref<Map<string, DraftPlanEntry>>;  // key: "YYYY-MM-DD|entryType"
          scoredRecipes: ComputedRef<ScoredRecipe[]>;
          recipeDataMap: ComputedRef<Map<string, RecipeFoodData>>;
          config: Ref<OptimizerConfigOut | null>;
          loading: Ref<boolean>;
          saving: Ref<boolean>;

          // Actions
          loadData(): Promise<void>;
          addToDraft(date: string, entryType: PlanEntryType, recipeId: string): void;
          removeFromDraft(key: string): void;
          clearDraft(): void;
          savePlan(): Promise<void>;           // diffs draft vs existing, creates/updates/deletes
          updateConfig(config: OptimizerConfigOut): Promise<void>;

          // Derived
          plannedRecipeIds: ComputedRef<Set<string>>;
          hasUnsavedChanges: ComputedRef<boolean>;
        }

        export function useOptimizerPlanner(): UsePlannerReturn { ... }
      depends_on:
        - "frontend/app/composables/optimizer/use-optimizer-scoring.ts"
        - "frontend/app/composables/use-group-mealplan.ts"
        - "frontend/app/lib/api/user/optimizer-pantry.ts"
        - "frontend/app/lib/api/types/meal-plan.ts"
        - "frontend/app/lib/api/types/optimizer.ts"
        - "frontend/app/composables/optimizer/types.ts"
      acceptance_criteria:
        - "loadData() fetches recipe-foods projection, pantry items, config, and existing meal plan entries for dateRange"
        - "Recipe-foods response is normalized (unwrap .items from RecipeFoodProjectionResponse)"
        - "Pantry items are mapped to PantryItemScoring[] for the scoring engine"
        - "Config weights are mapped to ScoringWeights for the scoring engine"
        - "addToDraft() inserts a DraftPlanEntry and triggers scoring recalculation via planned recipe tracking"
        - "removeFromDraft() removes entry and recalculates scores"
        - "savePlan() diffs draft against existing entries: creates new, updates changed, deletes removed"
        - "hasUnsavedChanges is true when draft differs from loaded state"
        - "scoredRecipes reactively updates when draft entries or config change"

    # ── Page ──
    - path: "frontend/app/pages/g/[groupSlug]/optimizer/planner.vue"
      action: create
      purpose: "Main optimizer planner page with two-panel layout: plan grid + suggestion sidebar"
      signature: |
        <template>
          <!-- v-container with two-panel responsive layout -->
          <!-- Left: date range picker + PlanGrid + save/shopping-list actions -->
          <!-- Right: ConfigPanel (collapsible) + SuggestionSidebar -->
          <!-- RecipeDialogAddToShoppingList (existing component) for shopping list generation -->
        </template>

        <script setup lang="ts">
        // Uses useOptimizerPlanner() composable for all state management
        // Uses useSeoMeta({ title: t("optimizer.planner.title") })
        // Uses useUserApi() for shopping list fetching
        // Provides shopping list dialog integration via RecipeDialogAddToShoppingList

        function onSlotClick(date: string, entryType: PlanEntryType): void { ... }
        function onSlotDrop(date: string, entryType: PlanEntryType, recipeId: string): void { ... }
        function onEntryRemove(key: string): void { ... }
        function onRecipeSelect(recipeId: string): void { ... }
        async function onSavePlan(): Promise<void> { ... }
        async function onGenerateShoppingList(): Promise<void> { ... }
        </script>
      depends_on:
        - "frontend/app/composables/optimizer/use-optimizer-planner.ts"
        - "frontend/app/components/optimizer/PlanGrid.vue"
        - "frontend/app/components/optimizer/SuggestionSidebar.vue"
        - "frontend/app/components/optimizer/ConfigPanel.vue"
        - "frontend/app/components/Domain/Recipe/RecipeDialogAddToShoppingList.vue"
      acceptance_criteria:
        - "Page renders at /g/{groupSlug}/optimizer/planner"
        - "Date range defaults to next 7 days, adjustable via date picker"
        - "Existing meal plan entries load into the grid on page load"
        - "Scoring recalculates reactively as recipes are added/removed from the plan"
        - "Save Plan button persists draft to meal plan API (create/update/delete diff)"
        - "Generate Shopping List opens existing dialog with planned recipes"
        - "Unsaved changes prompt or indicator prevents accidental navigation"

    # ── Components ──
    - path: "frontend/app/components/optimizer/PlanGrid.vue"
      action: create
      purpose: "7-day grid with entry-type rows; each cell is a PlanSlot"
      signature: |
        <script setup lang="ts">
        import type { PlanEntryType } from "~/lib/api/types/meal-plan";
        import type { DraftPlanEntry } from "~/composables/optimizer/use-optimizer-planner";

        const props = defineProps<{
          days: Date[];
          entries: Map<string, DraftPlanEntry>;  // key: "YYYY-MM-DD|entryType"
          entryTypes: PlanEntryType[];
          activeSlot: string | null;             // currently selected slot key for sidebar context
        }>();

        const emit = defineEmits<{
          (e: "slot-click", date: string, entryType: PlanEntryType): void;
          (e: "entry-remove", key: string): void;
        }>();
        </script>
      depends_on:
        - "frontend/app/components/optimizer/PlanSlot.vue"
        - "frontend/app/lib/api/types/meal-plan.ts"
        - "frontend/app/composables/optimizer/use-optimizer-planner.ts"
      acceptance_criteria:
        - "Renders columns for each day with date header"
        - "Renders rows for each entry type (breakfast, lunch, dinner, etc.) with i18n labels"
        - "Each cell renders a PlanSlot component"
        - "Filled slots show recipe name and thumbnail"
        - "Empty slots are visually distinct and clickable"
        - "Highlights the currently active slot"

    - path: "frontend/app/components/optimizer/PlanSlot.vue"
      action: create
      purpose: "Individual meal slot — shows recipe info or empty drop target"
      signature: |
        <script setup lang="ts">
        import type { DraftPlanEntry } from "~/composables/optimizer/use-optimizer-planner";

        const props = defineProps<{
          entry: DraftPlanEntry | null;
          slotKey: string;                       // "YYYY-MM-DD|entryType"
          isActive: boolean;
        }>();

        const emit = defineEmits<{
          (e: "click"): void;
          (e: "remove"): void;
        }>();

        // Note: drag-and-drop from sidebar uses manual drop handler, not VueDraggable
        // (VueDraggable expects v-model arrays; single-entry slots use native drag events)
        </script>
      depends_on:
        - "frontend/app/composables/optimizer/use-optimizer-planner.ts"
        - "frontend/app/components/Domain/Recipe/RecipeCardImage.vue"
      acceptance_criteria:
        - "Empty state: shows dashed border with + icon, clickable"
        - "Filled state: shows recipe name, small thumbnail via RecipeCardImage"
        - "Filled state: shows remove (X) button on hover"
        - "Active state: highlighted border when selected for sidebar context"
        - "Accepts drop events from sidebar recipe cards (native HTML drag-and-drop)"

    - path: "frontend/app/components/optimizer/SuggestionSidebar.vue"
      action: create
      purpose: "Scrollable ranked recipe list with search filter"
      signature: |
        <script setup lang="ts">
        import type { ScoredRecipe, RecipeFoodData } from "~/composables/optimizer/types";

        const props = defineProps<{
          scoredRecipes: ScoredRecipe[];
          recipeDataMap: Map<string, RecipeFoodData>;  // keyed by recipeId for O(1) lookup
          loading: boolean;
          activeEntryType: string | null;        // context hint: "Picking dinner for Tuesday"
        }>();

        const emit = defineEmits<{
          (e: "select", recipeId: string): void;
        }>();

        // Internal: search query ref for client-side filtering by recipe name
        const searchQuery: Ref<string> = ref("");
        const filteredRecipes: ComputedRef<ScoredRecipe[]> = computed(() => { ... });
        </script>
      depends_on:
        - "frontend/app/components/optimizer/OptimizerRecipeCard.vue"
        - "frontend/app/composables/optimizer/types.ts"
      acceptance_criteria:
        - "Displays recipes sorted by totalScore descending"
        - "Search input filters recipes by name (case-insensitive)"
        - "Shows context hint when a slot is active (e.g., 'Suggestions for Tuesday dinner')"
        - "Each recipe rendered as OptimizerRecipeCard"
        - "Shows loading skeleton while data loads"
        - "Shows empty state message when no recipes match search"
        - "Recipe cards are draggable (HTML drag) for drop onto PlanSlot"

    - path: "frontend/app/components/optimizer/OptimizerRecipeCard.vue"
      action: create
      purpose: "Compact recipe card with scoring breakdown and pantry match chips"
      signature: |
        <script setup lang="ts">
        import type { ScoredRecipe, RecipeFoodData, PantryMatchDetail } from "~/composables/optimizer/types";

        const props = defineProps<{
          scored: ScoredRecipe;
          recipe: RecipeFoodData;
        }>();

        const emit = defineEmits<{
          (e: "select", recipeId: string): void;
        }>();

        // Computed: format pantry match chips from scored.pantryMatches
        // Computed: format overlap percentage from scored.breakdown.overlap
        // Computed: format rating display from recipe.rating
        // Computed: format prep time from recipe.totalTime
        // HTML draggable attribute set, dragstart sets recipeId data
        </script>
      depends_on:
        - "frontend/app/composables/optimizer/types.ts"
        - "frontend/app/components/Domain/Recipe/RecipeCardImage.vue"
      acceptance_criteria:
        - "Shows recipe name and small thumbnail"
        - "Shows overlap percentage as a badge (e.g., '72% overlap')"
        - "Shows rating as stars or numeric display"
        - "Shows prep time if available"
        - "Shows pantry match chips: food name + urgency info (e.g., 'Cilantro - expires tomorrow')"
        - "High-priority pantry matches (expiring soon) are visually emphasized (e.g., warning color)"
        - "Card is HTML-draggable (draggable='true', @dragstart sets recipe data)"
        - "Click triggers select event"

    - path: "frontend/app/components/optimizer/ConfigPanel.vue"
      action: create
      purpose: "Collapsible panel with weight sliders wired to optimizer config API"
      signature: |
        <script setup lang="ts">
        import type { OptimizerConfigOut, OptimizerConfigUpdate } from "~/lib/api/types/optimizer";

        const props = defineProps<{
          config: OptimizerConfigOut | null;
        }>();

        const emit = defineEmits<{
          (e: "update", config: OptimizerConfigUpdate): void;
        }>();

        // Internal: localConfig reactive copy for editing
        // Debounced emit on slider change (500ms) to avoid excessive API calls
        // Expansion panel wrapping all controls
        </script>
      depends_on:
        - "frontend/app/lib/api/types/optimizer.ts"
      acceptance_criteria:
        - "Renders as a collapsible v-expansion-panel, collapsed by default"
        - "Shows labeled sliders for each weight: overlap, pantry utilization, pantry urgency, protein diversity, category balance, rating"
        - "Sliders range 0.0 to 2.0, step 0.1"
        - "Shows numeric input for prep time budget (minutes, nullable)"
        - "Emits debounced update event on any value change"
        - "Uses i18n keys from optimizer.config.* namespace (already exists)"

    # ── Modifications ──
    - path: "frontend/app/components/Layout/DefaultLayout.vue"
      action: modify
      purpose: "Add optimizer planner link to sidebar navigation"
      signature: |
        // In topLinks computed array, after the existing Pantry entry (~line 254), add:
        {
          icon: $globals.icons.calendarWeek,
          title: i18n.t("optimizer.planner.title"),
          to: `/g/${groupSlug.value}/optimizer/planner`,
          restricted: true,
        },
      depends_on:
        - "frontend/app/lang/messages/en-US.json"
      acceptance_criteria:
        - "Planner link appears in sidebar between Pantry and Timeline"
        - "Link uses calendarWeek icon"
        - "Link text is i18n-driven"
        - "Link is restricted to logged-in users"
        - "Link navigates to /g/{groupSlug}/optimizer/planner"

    - path: "frontend/app/lang/messages/en-US.json"
      action: modify
      purpose: "Add i18n keys for optimizer planner UI"
      signature: |
        // Add under "optimizer" object, after "config" block:
        "planner": {
          "title": "Meal Planner",
          "date-range": "Date Range",
          "save-plan": "Save Plan",
          "saving": "Saving...",
          "generate-shopping-list": "Generate Shopping List",
          "no-suggestions": "No recipe suggestions available",
          "search-recipes": "Search recipes...",
          "suggestions-for": "Suggestions for {entryType} on {date}",
          "overlap": "{percent}% ingredient overlap",
          "pantry-match": "Uses {count} pantry item | Uses {count} pantry items",
          "expires-in-days": "expires in {days} day | expires in {days} days",
          "expires-today": "expires today",
          "expired": "expired",
          "empty-slot": "Click to add or drag a recipe here",
          "unsaved-changes": "You have unsaved changes. Save before leaving?",
          "plan-saved": "Meal plan saved",
          "unlinked-recipes-hint": "{count} recipe has unlinked ingredients | {count} recipes have unlinked ingredients",
          "config-panel-title": "Scoring Weights"
        }
      depends_on: []
      acceptance_criteria:
        - "All planner UI text is i18n-driven"
        - "Pluralization works for pantry matches and expiration days"
        - "Keys are under optimizer.planner.* namespace"

handoff_to_deep_plan:
  skip_exploration:
    - "frontend/app/composables/optimizer/scoring-engine.ts — fully analyzed, 200+ lines, pure TS scoring"
    - "frontend/app/composables/optimizer/use-optimizer-scoring.ts — fully analyzed, thin Vue wrapper"
    - "frontend/app/composables/optimizer/types.ts — fully analyzed, all types documented"
    - "frontend/app/lib/api/user/optimizer-pantry.ts — fully analyzed, OptimizerApi class with all methods"
    - "frontend/app/lib/api/types/optimizer.ts — fully analyzed, all response types"
    - "frontend/app/lib/api/types/meal-plan.ts — fully analyzed, all plan types"
    - "frontend/app/composables/use-group-mealplan.ts — fully analyzed, useMealplans pattern"
    - "frontend/app/pages/g/[groupSlug]/optimizer/pantry.vue — fully analyzed, optimizer page pattern"
    - "frontend/app/components/optimizer/PantryItemRow.vue — fully analyzed, component pattern"
    - "frontend/app/components/Layout/DefaultLayout.vue — fully analyzed, topLinks at lines 224-292"
    - "frontend/app/pages/household/mealplan/planner.vue — fully analyzed, parent planner pattern"
    - "frontend/app/pages/household/mealplan/planner/edit.vue — fully analyzed, VueDraggable pattern"
    - "frontend/app/pages/household/mealplan/planner/view.vue — fully analyzed, day/section display"
  known_patterns:
    - "Optimizer pages: /g/[groupSlug]/optimizer/<feature>.vue with useSeoMeta + useUserApi"
    - "Optimizer components: frontend/app/components/optimizer/<Name>.vue with defineProps + defineEmits"
    - "Composable pattern: reactive refs + computed + API calls, exported function returning object"
    - "Sidebar links: SideBarLink objects in topLinks computed, restricted: true for auth"
    - "VueDraggable: used for reordering within lists; for cross-component drag-drop (sidebar→grid), use native HTML drag-and-drop"
    - "i18n: keys under optimizer.<feature>.* in en-US.json, use $t() in templates"
    - "Meal plan CRUD: useMealplans(range) watches range ref, auto-refreshes"
    - "RecipeCardImage: takes recipeId, slug, tiny/small size props"
    - "Date formatting: date-fns format() for API, $d() for display"
  decisions_made:
    - "Native HTML drag-and-drop for sidebar→grid: VueDraggable expects v-model arrays but slots are single-entry; native drag events are simpler and avoid adapter complexity"
    - "Draft-based editing model: DraftPlanEntry map keyed by date|entryType, diffed against existing ReadPlanEntry[] on save to determine create/update/delete operations"
    - "Single composable (use-optimizer-planner) owns all data loading and normalization: unwraps RecipeFoodProjectionResponse.items, maps PantryItemOut[] to PantryItemScoring[], maps OptimizerConfigOut to ScoringWeights — single source of truth"
    - "Config changes debounced at 500ms in ConfigPanel, persisted via API on each change (not save-plan)"
    - "Shopping list generation reuses RecipeDialogAddToShoppingList verbatim — no custom implementation"
    - "Planner link placed between Pantry and Timeline in sidebar, using calendarWeek icon"
  warnings:
    - "useMealplans(range) auto-watches range and refreshes — the planner must coordinate to avoid double-fetching when loading initial data"
    - "RecipeFoodProjectionResponse must be unwrapped (.items) before passing to scoring engine — easy to miss"
    - "savePlan() must handle the full diff: new entries → createOne, changed entries → updateOne, removed entries → deleteOne — partial save could leave stale entries"
    - "PlanSlot drop handler must validate that recipeId from drag data exists in recipeDataMap before adding to draft"
    - "Scoring recalculation happens on every draft change — with 500+ recipes this should be fast (O(n) set lookups) but profile if UI feels sluggish"
    - "Entry types are hardcoded as 7 values in PlanEntryType — the grid should only show types the user has configured or a sensible default subset (breakfast, lunch, dinner)"

open_questions:
  - question: "Should the grid show all 7 entry types by default, or a configurable subset (e.g., just breakfast/lunch/dinner)?"
    blocking: false
    default_assumption: "Show breakfast, lunch, and dinner by default; other types available via an 'Add row' control"
  - question: "Should clicking an empty slot auto-fill with the top suggestion, or just highlight the slot for manual selection from the sidebar?"
    blocking: false
    default_assumption: "Highlight the slot and scroll sidebar to top — user explicitly selects"
  - question: "Should there be an undo/redo for draft changes?"
    blocking: false
    default_assumption: "No — draft changes are lightweight; Clear Draft button is sufficient"
  - question: "How should the page handle the case where Stage A endpoints (config, recipe-foods) are not deployed yet?"
    blocking: false
    default_assumption: "Show error state with clear message if API calls fail — the spec assumes Stage A is complete"

agent_responses:
  codex_verdict: CONCERNS
  codex_notes: |
    Verdict: CONCERNS

    Specific issues found:
    - `api.optimizer.getRecipeFoods()` returns a `RecipeFoodProjectionResponse`, not a raw array;
      planner/composable must unwrap `.items` before passing into scoring, or scoring will fail at runtime.
    - `useMealplans(range)` already watches and refreshes on `range` changes; a planner that also calls
      `actions.refreshAll()` on range changes risks double fetches unless carefully coordinated.
    - "Save Plan" is specified as `api.mealplans.createOne()` only; existing entries for the range need
      update/delete logic to avoid duplicate entries and stale plan state.
    - `PlanSlot` drag/drop: VueDraggable expects `v-model` with an array, but your `PlanSlot` props suggest
      a single entry; you'll need an adapter layer or manual drop handling.
    - `SuggestionSidebar` props include `recipeFoodData` and `scoredRecipes`; you need a deterministic
      join by `recipeId` and an empty-state strategy.

    Recommended changes:
    - Adjust planner/composable to normalize API responses (`RecipeFoodProjectionResponse → RecipeFoodData[]`)
      in a single place (`use-optimizer-planner.ts`) and pass normalized data down.
    - Define a clear draft data model: `DraftPlanEntry` keyed by `date+entryType`, then diff against
      existing `ReadPlanEntry` on Save to decide create/update/delete.
    - For drag/drop, either: (1) make each slot own a one-item array for VueDraggable, or (2) keep manual
      `drop` handlers and avoid VueDraggable inside `PlanSlot`.
    - Add an explicit mapping step (`Map<recipeId, RecipeFoodData>`) in the composable so
      `OptimizerRecipeCard` always has both `ScoredRecipe` and matching recipe data.

    All concerns have been addressed in this specification:
    - Response normalization handled in use-optimizer-planner.ts (single source of truth)
    - DraftPlanEntry model with create/update/delete diff on save
    - Native HTML drag-and-drop instead of VueDraggable for single-entry slots
    - recipeDataMap (Map<string, RecipeFoodData>) exposed from composable for O(1) lookup
```
